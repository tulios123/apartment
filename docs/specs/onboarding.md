# הדקה הראשונה — האפיון החי

> **המסמך היחיד** לנושא הזה. כל החלטה נרשמת כאן ביום שהתקבלה, וכל שאלה פתוחה
> יושבת כאן עד שנסגרת. אין מסמך שני. (החלפנו כך אחרי שהבעלים אמר, 06.09:
> "אנחנו צריכים להמשיך לאפיין וזה מרגיש לי נאבד".)

---

## יומן-ההחלטות

| תאריך | הוכרע | מי |
|---|---|---|
| 06.09 | **האפליקציה תביא ערך בכל שלב של הדרך**, לא רק כשהדירה מושכרת. זו הכרעת "דרך ב׳" — המוצר מכסה את הקשת רכישה ← מסירה ← השכרה | בעלים |
| 06.09 | **האשף לא יהיה שער.** חייבת להיות דרך להגיע מיד למסך-הבית | בעלים |
| 06.09 | מצב **"גר בדירה בעצמי" — בחוץ**. זו אפליקציה לניהול השקעה | בעלים |
| 06.09 | תזכורת "אין חוזה שכירות" **מושתקת** למי שטרם קיבל מפתח | קלוד (אושר) |
| 07.09 | התיקון **פורסם ל-production** — ההתראה מפסיקה להישלח | בעלים |
| 07.09 | **הקשת מתארכת אחורה: גם "על סף רכישה"** — ושם הערך הוא **הסימולטור** (מכירה מול החזקה, שינויי-ריבית, מיחזור). היה עד היום ב"בהמשך" במפה | בעלים |
| 07.09 | **תפקיד מסך-הבית לפני מסירה: להכניס ודאות** — תחזית, משימות, ולעבור את השלב. אופיין בחלק ט׳ | בעלים |
| 08.09 | **שני מצבים, לא שלושה.** "יש מפתח, אין שוכר" הוא וריאציה של "מושכרת", לא מצב בפני עצמו — התזרים וההכנסות-הוצאות כבר רלוונטיים שם. הגבול היחיד הוא **המפתח** | בעלים |
| 07.09 | **טוען-התרחישים עובר לתפריט-הפרופיל** — לא דרך ההגדרות | בעלים |
| 07.09 | **אין מסגרת-תזרים במצב טרם-מסירה** — אין זרימה חודשית, ולכן גם אין קישור לפירוט | בעלים |
| 08.09 | **מותר לעבוד ישירות על staging**, ולהוסיף **בדיקות עיצוב** אוטומטיות | בעלים |
| 08.09 | **באג ההון מתוקן ונדחף** — פיצול של חודש עתידי לא ייאמר בלשון הווה | בעלים |
| 08.09 | **את התהליך לומדים ממקורות, לא ממציאים.** אם המחקר ייתן תמונה ברורה — מותר אב-טיפוס של הצגה, מאחורי הסטגינג | בעלים |

## פתוח — כל שאלה עם האפשרויות והמחיר שלהן

*(נכתב מחדש 09.09. קודם זו הייתה רשימת נושאים; אי-אפשר להכריע נושא. עכשיו כל שורה
היא שאלה אחת עם אפשרויות — כך אפשר לענות בשתי מילים ולהמשיך.)*

| # | השאלה | אפשרות א׳ | אפשרות ב׳ | מה המחיר |
|---|---|---|---|---|
| 1 | **שני שעונים או אחד?** מס רכישה נספר מ**החתימה** (30/60 יום), כל השאר מ**המסירה** | להוסיף עוגן-חתימה | להישאר על עוגן-מסירה בלבד | א׳: מי שנרשם מאוחר רואה מועד שכבר עבר — צריך ניסוח ל"עבר". ב׳: מוותרים על המועד המחייב היחיד שאנחנו יכולים לחשב |
| 2 | **מה עושים עם משימה שממתינה לצד שלישי?** (בנק, עו״ד, קבלן) | משימה רגילה עם ✓ | **מצב**: נשלח → חזר | ב׳ הוא מודל חדש (שדה/סטטוס), אבל זה מה שבאמת קורה. א׳ זול ומשקר קצת |
| 3 | **קבלן או יד שנייה — שואלים?** | שאלה אחת בהצטרפות | לא שואלים | א׳: מחיר של שאלה אחת (חוק היק), פותח ~שליש מהתהליך (מדד, ערבויות, שנת בדק). ב׳: מכנה משותף דל יותר |
| 4 | **הכותרת של מסך-הבית** — "צפי לסוף החודש" | להשאיר תזרים | להחליף לרווח אמיתי / הון שנבנה | הכי גלוי באפליקציה; ראו סיור-המוחות ממצא 1 |
| 5 | **דחיסת מרכז-הפעולות** — ביום עמוס ארבעה כרטיסים תופסים שני מסכים | לדחוס לשורות פרט לראשון | להשאיר | סיור-המוחות ממצא 4 |
| 6 | **צורת כרטיס טרם-המסירה** — מדווח, לא משדר מצב | ספירה + פס כגוף הכרטיס | להשאיר רשימת-שורות | סיור-המוחות ממצא 5; כאן הרפרנסים שביקשת ישנו הכי הרבה |
| 7 | **קיצור הליבה** — מה יוצא מהאשף | — | — | **מוקפא בבקשתך** (06.09, "רגע") |
| 8 | **שלב "על סף רכישה" + הסימולטור** | — | — | אפיון נפרד, אחרי שהשלב הזה עומד |

**נסגרו:** מה הבית מראה לפני מסירה (ט׳) · שני מצבים ולא שלושה (08.09) · "איפה אתה"
מתמוסס לתוך שדה-התאריך · טוען-התרחישים · אין מסגרת-תזרים לפני מסירה.

## הצעות שטרם אושרו
- **המצב נגזר, לא נשמר** — מתאריך-מסירת-המפתח ומקיום חוזה פעיל. בלי מיגרציה,
  ובלי שאף משתמש קיים משנה התנהגות. (פירוט בחלק ו׳.)

---

## תקציר בשבע שורות

האפליקציה בנויה סביב **שעון אחד** — המחזור החודשי: שכר-דירה נכנס, תשלומים קבועים
יוצאים, החודש נסגר, וחוזר. כל ארבעת עמודי-התווך משרתים אותו.

לאדם שקנה דירה וטרם קיבל מפתח **אין מחזור חודשי**. לכן זה לא שארבעה שלבים באשף לא
רלוונטיים לו — **האפליקציה כולה לא רלוונטית לו, בינתיים.** האשף הוא רק המקום שבו זה
נתקל בו ראשון.

מכאן: תיקון האשף לבדו רק יזרז אותו אל מסך ריק. ההחלטה האמיתית היא **האם האפליקציה
מכסה את הקשת המלאה — רכישה ← מסירה ← השכרה — או רק את קצה-ההשכרה.** כל השאר נגזר.

---

# חלק א׳ — מה אנחנו יודעים על חוויית-משתמש

יש לנו קאנון כתוב ומקוּור: `docs/audit/UX_FOUNDATIONS.md` (נאסף 16.07 ממקורות
ראשוניים — NN/g, Laws of UX, Apple HIG, GOV.UK, Polaris, WCAG). להלן חמשת הכללים
שחלים על מסך-פתיחה של אפליקציה, ומה כל אחד אומר עלינו.

### 1. המכשיר הנכון לאשף הוא "מסע-קוגניטיבי", לא "הערכה היוריסטית"
*(פרק 3)* — ההבדל מפורש אצלנו במסמך: הערכה היוריסטית שופטת ממבט-האנליסט את
השימושיות הכללית; **מסע-קוגניטיבי בוחן לְמִידוּת מנקודת-מבטו של משתמש חדש**, צעד
אחר צעד, בארבע שאלות:

1. האם המשתמש ינסה להשיג את התוצאה הנכונה?
2. האם ישים לב שהפעולה הנכונה זמינה?
3. האם יקשר בין הפעולה לתוצאה שהוא רוצה?
4. **אחרי הפעולה — האם יראה שהתקדם לעבר המטרה?**

אם אחת התשובות "לא" — הצעד נכשל. זה בדיוק המכשיר לדקה הראשונה, והוא מה שהרצתי
בחלק ד׳.

### 2. חוק היק — כל בחירה מיותרת עולה בזמן ובביטחון
*(פרק 2)* — "זמן ההחלטה גדל עם מספר האפשרויות ומורכבותן. המסקנה: לצמצם אפשרויות
כשמהירות חשובה, לפרק משימה מורכבת לשלבים, ולהדגיש ברירת-מחדל מומלצת."

אצלנו: משתמש עובר **שבעה מסכים וכ-30 שדות** לפני שהוא רואה ערך כלשהו.

### 3. GOV.UK — לשאול רק את מה שבאמת צריך, ולדעת למה
*(פרק 5)* — "שאלו רק מה שאתם באמת צריכים; דעו למה אתם שואלים כל שאלה." בנוסף:
שאלה אחת לעמוד · לעולם לא לשאול פעמיים · לסמן "(אופציונלי)" ולא לסמן חובה בכוכבית ·
לכתוב "המשך" ולא "הבא".

הכלל הראשון הוא החשוב כאן, והוא היחיד שאנחנו מפרים בגדול: **האשף מבקש הכול מראש,
כי כך נוח למסד-הנתונים — לא כי כך צריך לאדם.**

### 4. חוק יעקב — אנשים מצפים שזה יעבוד כמו כל השאר
*(פרק 2)* — "משתמשים מבלים את רוב זמנם באתרים אחרים, ולכן מעדיפים שהאתר שלך יעבוד
כמו כל האחרים שהם כבר מכירים."

המשמעות לשלב-הבחירה: **מסך "איפה אתה בתהליך" הוא דפוס מוכר** מאפליקציות בנקאות,
בריאות והגירה. אנחנו לא ממציאים משהו — אנחנו מיישרים לציפייה קיימת.

### 5. חוק השיא-והסוף — נשפטים לפי הרגע החזק ולפי הסיום
*(פרק 2)* — "אנשים שופטים חוויה לפי הרגע העוצמתי ביותר ולפי סופה, לא לפי הממוצע.
רגעים שליליים נזכרים בחיוּת רבה יותר מחיוביים. המסקנה: להשקיע את מאמץ-העיצוב
בנקודות-השיא הרגשיות וברגע האחרון של הזרימה."

והנה הנקודה הכי חשובה במסמך הזה: **הסוף של האשף אינו מסך-הסיום. הסוף הוא מסך-הבית
שנפתח אחריו.** אף אחד לא זוכר את הקונפטי; זוכרים את המסך הראשון האמיתי.

---

# חלק ב׳ — מטרת האפליקציה, ומה מתנגש בה

מסמך-המוצר שלנו (`docs/handoff/overview.md`, שורה ראשונה):

> אפליקציית ווב לניהול השקעה **בדירה אחת מושכרת** … שרוצה לראות במקום אחד: תזרים
> חודשי, הון עצמי וצבירת-בעלות, מבנה המימון, חוזי-שכירות, ביטוח, משימות ומסמכים.

המילה "מושכרת" אינה מקרית — היא מתארת נאמנה את מה שנבנה. **יש לאפליקציה שעון אחד:
המחזור החודשי.** כל עמוד-תווך משרת אותו:

| עמוד | תפקידו במחזור |
|---|---|
| ראשי | מה דורש אותך **החודש** |
| תזרים | איך נראה **החודש** |
| הון | מה הצטבר **לאורך החודשים** |
| ניהול הנכס | החוזים שמייצרים את המחזור |

**לרוכש שטרם קיבל מפתח אין מחזור חודשי.** אין הכנסה, אין שוכר, אין מה לאשר, ולעתים
קרובות גם המשכנתא טרם החלה. הוא אינו "מקרה-קצה של משתמש" — הוא **מחוץ למטרה
המוצהרת של המוצר**.

זו הסיבה שהאפיון הקודם היה לא-טוב: הוא טיפל בזה כבעיית-ניווט באשף. זו לא בעיית
ניווט. זו שאלה על גבולות המוצר.

---

# חלק ג׳ — הצורך, לפי אדם

### איתי (וכל המשפחה היום) — יש דירה, יש שוכר
**מה הוא צריך מהדקה הראשונה:** אמינוּת. "הדבר הזה מבין את הדירה שלי."
**איך זה מושג היום:** מסך-הסיום מחזיר לו את המספרים שלו — שווי, משכנתא, שכר-דירה,
הון. זה עובד. **לא לשבור.**

### האח והחבר — קנו, המפתח עוד לא אצלם
מה קורה אצלם בפועל בחודשים האלה:
- כסף **יוצא** במנות גדולות: תשלומים למוכר/לקבלן, מס-רכישה, עו״ד, תיווך, יועץ-משכנתאות.
- החלטות פתוחות: איזו משכנתא, מתי לסגור ביטוח, מתי להתחיל לחפש דייר.
- חרדה אחת ממשית: **"כמה מהכסף שלי כבר בפנים, ומה עוד נשאר לשלם?"**
- ואפס דבר שחוזר על עצמו כל חודש.

**מה הדקה הראשונה יכולה להבטיח להם:** לא לוח-מחוונים — אין ממה לבנות אותו. הדבר
היחיד שהאפליקציה יכולה להציע בכנות הוא: **"אני יודעת איפה אתה בתהליך, ואהיה מוכנה
כשתהיה."**

וזו הסיבה שהנדנוד הדו-שבועי (חלק ד׳) אינו באג קטן: **זו האפליקציה מפרה את ההבטחה
הזו, בדיוק, כל 14 יום.**

הערה חשובה: את החומר לצורך שלהם **אנחנו כבר אוספים** — שלב "השקעה" באשף כבר מבקש
הון-עצמי, עלויות-נלוות ובלון, ומסך-ההון כבר יודע להציג צבירה. לא חסר לנו מנוע. חסר
לנו שעון שני שיכוון אליו את החומר הקיים.

### מצב שלישי — יש מפתח, אין עדיין שוכר
בין-לבין: הדירה בידיים, אין הכנסה. כאן האפליקציה **כן** רלוונטית (יש הוצאות קבועות,
יש משכנתא רצה, יש מה לעשות), והמסר "אין חוזה — הוסיפו שוכר" הוא נכון ופעיל. זה
המצב היחיד מהשלושה שהיום מטופל סביר.

---

# חלק ד׳ — הראיות: מסע-קוגניטיבי על האשף הקיים

הרצתי את ארבע השאלות של פרק 3 על כל צעד. מסומן ✗ היכן שהתשובה "לא".

### המסלול של איתי (מושכרת)
| צעד | ממצא |
|---|---|
| פתיחה → מסמכים | ✗ **ש״4:** מסך-המסמכים אינו נספר. הנקודות ומונה "שלב N מתוך M" נגזרים מ-`STEP_ORDER` שכולל שישה שלבים בלבד — מסך-המסמכים בא לפניהם ואינו מופיע בהם. המשתמש עובר מסך, ואז מתבשר שהוא ב"שלב 1 מתוך 6" |
| רכישה → ביטוח | ✗ **ש״4 (מצטבר):** שישה מסכים, כ-30 שדות, ואפס משוב על ערך. ההתקדמות נמדדת במסכים שנותרו, לא בדבר שנבנה |
| סיום | ✔ עובד — המספרים חוזרים אליו. זה השיא, והוא אמיתי |

### המסלול של האח והחבר (טרם מפתח)
| צעד | ממצא |
|---|---|
| פתיחה | ✗ **ש״1:** רשימת-הנושאים מסתיימת ב"**הכנסות** — שכירות, תזכורות וביטוח" (`WelcomeStep.tsx:13`). בשנייה העשירית הוא קורא "זו אפליקציה למשכירים", ואין לו עדיין מה להשכיר. הספק נזרע לפני שהוא הקליד תו |
| שכירות (שלב 5) | ✗ **ש״1:** שלב שלם על משהו שלא קיים. הוא כתוב "אופציונלי", אבל עדיין צריך להיפתח, להיקרא ולהיות מוכרע. חוק היק — עלות בלי תמורה |
| סיום | ✗ **ש״4:** "הכול מוכן! הנכס שלך מוגדר — **אפשר להתחיל לנהל**" (`DoneStep.tsx:35-36`). אין מה לנהל |
| **המסך שאחריו** | ✗✗ **ש״4 — הכישלון החמור ביותר:** מסך-הבית פותח בכרטיס "**אין חוזה שכירות פעיל** · הוסיפו שוכר כדי לעקוב אחרי ההכנסה החודשית" (`HomeScreen.tsx:501-508`). לפי חוק השיא-והסוף זה **הסוף האמיתי** של הדקה הראשונה — והוא הודעת-חוסר על משהו שהוא לא יכול לתקן |
| כל 14 יום, לתמיד | ✗ התראת-פוש: "אין חוזה שכירות פעיל — מומלץ להוסיף שוכר חדש" (`daily-reminders/index.ts:179-186`, `NO_LEASE_REPEAT_DAYS = 14`). מסירה בעוד שמונה חודשים = כ-17 התראות שממליצות להשכיר דירה שאי אפשר להיכנס אליה |
| מסך ההון | ○ **תוקן במסמך 06.09:** טענתי כאן שמוצגת תשואה 0%. בדיקה בקוד מראה שלא — `WealthHub.tsx:54,68-70` מגן על כל התשואות (`monthlyRent > 0 ? … : null`) ומקטע "תשואות" כולו נעלם. אין מספר שגוי; יש **שתיקה**. האם להחליף אותה ב"טרם החלה הכנסה" היא החלטת-עיצוב, לא תיקון-באג — ומקומה עם מסך-הבית של טרם-מפתח |

**מסקנת המסע:** אצל איתי האשף נכשל בשאלה 4 בלבד, ובעדינות — הוא ארוך, לא שבור.
אצל האח והחבר הוא נכשל בשאלה 1 בשנייה העשירית, ובשאלה 4 בכל נקודת-סיום שיש.

---

# חלק ה׳ — שלוש דרכים, והמלצה

### דרך א׳ — חדר-המתנה
המטרה נשארת "דירה מושכרת". מי שטרם קיבל מפתח: האשף אוסף מה שהוא יודע, **שותק**
(אפס נדנודים), מציג ספירה למסירה, וביום המפתח הופך לאפליקציה האמיתית.
*עלות:* קטנה · *כנות:* גבוהה · *סיכון:* הם ישכחו שהיא קיימת תוך חודשיים.

### דרך ב׳ — להרחיב את המטרה לקשת המלאה
האפליקציה מכסה **רכישה ← מסירה ← השכרה**. לשלב שלפני-המפתח יש שעון משלו: ספירה,
רשימת-הכנות, ובעיקר **"כמה מהכסף שלי כבר בפנים ומה נשאר לשלם"** — מהחומר שאנחנו
כבר אוספים.
*עלות:* בינונית · *תמורה:* הם משתמשים מהיום הראשון · *משמעות:* המטרה המוצהרת
משתנה מ"דירה מושכרת" ל"השקעה בדירה".

### דרך ג׳ — לומר להם בכנות "לא עכשיו"
מסך אחד: "האפליקציה מיועדת לדירה מושכרת. נשמור את הפרטים שלך ונודיע לך במסירה."
*עלות:* אפסית · *תוצאה:* הם עוזבים.

### ✅ הוכרע (בעלים, 06.09): דרך ב׳
> "אני חושב שצריך שהאפליקציה תביא ערך לכל שלב, והיא גם יכולה — יש הרבה משימות
> לאורך הדרך."

והמשפט הזה גם פותר את הבעיה הגדולה של דרך ב׳. חששתי שהיא דורשת "שעון שני" —
מנגנון חדש לשלב שלפני-המפתח. **היא לא. מנוע-המשימות הוא השעון השני, והוא כבר בנוי
ומחובר לכל מה שצריך:**

| מה שכבר קיים | איפה |
|---|---|
| למשימה יש תאריך ושעה, קטגוריה, וחזרתיות | `tasks` |
| משימות פתוחות/באיחור עולות ל**מרכז-הפעולות במסך-הבית** | `HomeScreen.tsx` — רשימת ה-actions |
| משימה שמועדה הגיע נשלחת ב**פוש היומי** | `daily-reminders/index.ts` — סעיף 3 |
| סימון "בוצע" מציע לרשום תנועה כספית מתאימה | מנוע-המשימות |

כלומר: לרוכש שטרם קיבל מפתח לא חסר מסך חדש — **חסרות לו משימות.** ברגע שיש לו
שש-שבע משימות אמיתיות עם תאריכים (אישור-משכנתא, ביטוח, בדיקת-ליקויים, חיפוש דייר,
תשלום למוכר), מסך-הבית הקיים מתמלא מעצמו בדברים נכונים, והפוש היומי הופך מנדנוד
ל**עזרה**.

זה משנה את גודל העבודה: מ"לבנות שלב-חיים שני במוצר" ל"**לזרוע את המשימות הנכונות
ולנסח את כרטיס-הכסף לפי המצב**".

---

# חלק ו׳ — מהלך-הדברים המוצע

### העיקרון שמחליף את הקיים
היום האשף מנסה **למלא את מסד-הנתונים**. מהיום תפקידו:

> **להביא את האדם, במהירות האפשרית, למסך ראשון שאומר עליו משהו נכון —
> ולתת לאפליקציה להרוויח את שאר הפרטים בהמשך, בהקשר.**

זה יישום ישיר של GOV.UK ("רק מה שבאמת צריך, ולדעת למה") ושל חוק היק.

### ומה שנגזר מזה: האשף מפסיק להיות שער
**הוכרע (בעלים, 06.09):** "באמת חייבים לעשות שאפשר ישר להגיע למסך הבית."

היום "כניסה לאפליקציה" קיימת רק בסוף, ו"סיימו עכשיו" מופיע רק בתחתית השלבים
האופציונליים (`FinishEarly.tsx`). כלומר האשף הוא **שער**: עוברים אותו, או לא נכנסים.

מהיום הוא **מדרגות נעות שאפשר לרדת מהן בכל שלב.** ברגע שיש מינימום שהופך מסך
אחד לנכון, הכניסה לאפליקציה זמינה מכל מסך באשף — לא כקישור-מילוט קטן בתחתית, אלא
כאפשרות שוות-מעמד.

זה גם התשובה לשאלה "ומה עם מי שכבר יש לו את כל הפרטים?" — הוא מקבל **שני מסלולים
לאותם נתונים**:

| | מסלול |
|---|---|
| **מודרך** | ממשיך באשף עד הסוף. מהיר, עם מילוי-אוטומטי ממסמכים, ומסיים עם תמונה מלאה |
| **קצר** | נכנס עכשיו, והאפליקציה מזמינה אותו להשלים בהקשר — כשהוא פותח את מסך-הביטוח, כשמגיע חוזה, כשצריך |

זה בדיוק מה שהיוריסטיקה 7 בקאנון שלנו קוראת לו *"גמישות ויעילות: שהמתחיל יצליח
בדרך הפשוטה, ושלמנוסה יהיו קיצורי-דרך — כל עוד המסלול המהיר לא עומד בדרכו של
המתחיל"*.

### מה זה אומר בפועל — ארבעה מהלכים

**1 · שאלה אחת בהתחלה: איפה אתה בתהליך**
שלוש תשובות, כל אחת אומרת גם מה יקרה בעקבותיה (ברגע שבחירה משנה התנהגות, ניסוח
מעורפל גורם לאדם לבחור לא-נכון ולגלות חודש אחר-כך):
- הדירה מושכרת — יש שוכר, או חוזה חתום שמתחיל בקרוב
- קניתי — המפתח עוד לא אצלי
- המפתח אצלי — עדיין אין שוכר

*(חוק יעקב: דפוס מוכר. חוק היק: שלוש אפשרויות, לא שש.)*

**2 · ליבה קצרה — רק מה שהופך את המסך הראשון לנכון**
| מצב | הליבה | מה זה מייצר |
|---|---|---|
| מושכרת | כתובת · שכר-דירה · תאריכי-חוזה · משכנתא | מסך-בית אמיתי עם מחזור חודשי |
| טרם-מפתח | כתובת · מחיר · תאריך-מסירה · הון שנכנס | ספירה + "כמה כבר בפנים" |
| יש מפתח, אין שוכר | כתובת · מחיר · משכנתא | תזרים-הוצאות אמיתי |

כל השאר — ביטוח, הלוואות, עלויות-נלוות, מסמכים — **יורד מהאשף** ומוזמן מתוך
האפליקציה כשהוא רלוונטי. זה המקום שבו "שבעה מסכים" הופכים לשלושה.

**3 · סיום שאומר אמת, וממשיך למסך שעובד**
מסך-הסיום מנוסח לפי המצב, ומעביר למסך-בית שמתאים לו. עבור טרם-מפתח: לא "אפשר
להתחיל לנהל" אלא מה שבאמת נכון — כמה נכנס, מתי המפתח, מה הצעד הבא.

**4 · האפליקציה מפסיקה לבקש את מה שאי אפשר לתת**
השתקת ההתראה הדו-שבועית לפני מסירה · ניסוח מסך-הבית לפי המצב · ובמקום מקטע-תשואות
שנעלם בשקט, אמירה מפורשת ("טרם החלה הכנסה") — אם נחליט שכך.

### איך המצב נשמר — ולמה זה לא דורש מיגרציה
המצב **נגזר משני נתונים שכבר קיימים**: תאריך-מסירת-המפתח (שדה קיים,
`properties.key_delivery_date`) והאם יש חוזה פעיל (`activeContract()`).
הכלל: **"טרם-מסירה" רק אם מולא תאריך-מסירה עתידי; כל השאר — הדירה ברשותך.**

שתי תוצאות נעימות: ביום שהתאריך עובר המצב **מתהפך מעצמו** ואין מנגנון-מעבר
לתחזק; וכל המשתמשים הקיימים נקראים "ברשותך" — כלומר **אף אחד מהם לא משנה
התנהגות, ואין אונבורדינג מחדש.**

### סדר ביצוע
| # | מה | תלוי בהחלטת-עיצוב? |
|---|---|---|
| 1 | השתקת הנדנוד הדו-שבועי לפני מסירה | **לא** — תנאי אחד בקוד · **בוצע 06.09** |
| 2 | שאלת "איפה אתה" + מסלול לפי מצב | ניסוח קיים, עיצוב לא |
| 3 | קיצור הליבה (ביטוח/הלוואות/מסמכים יוצאים מהאשף) | כן — מה יורד ומאיפה מוזמן |
| 4 | מסך-הבית של טרם-מפתח | **כן — זו ההחלטה הגדולה** |
| 5 | ברכת "המפתח שלך" ביום המסירה | לא |

**1 שוחרר בנפרד מהכול** — הוא באג פעיל שנשלח כרגע לאנשים אמיתיים, ואינו תלוי בשום
החלטת-עיצוב.

---

# חלק ז׳ — הצעד הבא באפיון

יומן-ההחלטות והרשימה הפתוחה נמצאים **בראש המסמך**, לא כאן. מה שנשאר כאן הוא רק
מה שאני מציע לאפיין בסיבוב הבא, כדי שהעבודה תתקדם בלי להמתין לרפרנסים:

**רשימת-המשימות של כל שלב** (פתוח #3). זו העבודה היחידה שלא חסומה, והיא זו
שהופכת את הכרעת "ערך בכל שלב" למשהו ממשי. צריך לקבוע, לכל שלב:
- אילו משימות נזרעות, ומתי (יחסית לתאריך-המסירה או לתאריך-החוזה)
- מה קורה כשמסמנים "בוצע" — האם נפתחת תנועה כספית, שדה למילוי, או כלום
- מה **לא** נזרע, כדי שהמסך לא יהפוך לרשימת-מטלות מלחיצה ביום הראשון

**מה שיעזור מאוד ואינו תלוי בי:** לשאול את האח והחבר איפה הם נתקעו ומה ציפו
שהאפליקציה תעשה בשבילם. זה שווה יותר מכל השערה שלי, והוא גם התשובה הישירה לפתוח #2.

---

# נספח — מה המסמך הזה לא מכסה
- עיצוב חזותי: פריסה, צבע, איורים, אנימציה. ממתין לרפרנסים.
- שמירה מתקדמת באשף (היום יש טיוטה ב-localStorage, `useOnboardingState.ts:90-95`,
  והיא עובדת) — לא נדרש שינוי בשלב זה.
- ריבוי-נכסים למשתמש. האפליקציה מניחה נכס אחד, וזה מחוץ לדיון.

---

# חלק ח׳ — אפיון המשימות (07.09)

הבסיס: הכרעת "ערך בכל שלב", והנימוק של הבעלים — "יש הרבה משימות לאורך הדרך".
כאן נקבע אילו משימות, מתי, ומה קורה כשמסמנים "בוצע".

## ח.1 שני ממצאים במנגנון — לפני שמדברים על תוכן

**(א) חלון-התצוגה מכוויל לחודש, לא למסע.**
`TASK_HOME_LEAD_DAYS = 2` (`src/lib/constants.ts:19`) — משימה עם תאריך עתידי מוסתרת
ממסך-הבית עד יומיים לפניה (`homeTasks.ts:28-32`). זה מצוין למחזור החודשי, ו**הרסני
למסע של חצי שנה**: לרוכש שממתין למפתח מסך-הבית יהיה ריק במשך חודשים, ואז תקפוץ
משימה יומיים לפני. כלומר גם אם נזרע את המשימות המושלמות, האפליקציה תמשיך להיראות
ריקה — וזו בדיוק הבעיה שבאנו לפתור.

**(ב) הפתרון כבר כתוב ולא בשימוש.**
`nextScheduledTask()` (`homeTasks.ts:52`) מחזיר את המשימה העתידית הקרובה ביותר.
מסך-הבית מייבא רק את הספירה (`futureTaskCount`, `HomeScreen.tsx:144`) ומציג
"עוד N משימות בעתיד" — מספר, בלי שֵם.

**ההצעה:** במצב-מסע, מסך-הבית מציג את **המשימה הבאה בשמה ובתאריכה** במקום ספירה.
זה שינוי קטן שמשתמש בפונקציה קיימת, והוא ההבדל בין מסך ריק לבין "אני יודע מה הצעד
הבא ומתי".

## ח.2 החוק שמונע רשימת-מטלות מלחיצה

> **משימה נזרעת רק אם יש לה עוגן-תאריך. משימה בלי תאריך — לא נזרעת.**

הסיבה מכנית: משימה חסרת-תאריך **תמיד** גלויה בבית (`homeTasks.ts:30`, התנאי
`!t.due_date`). זריעה של שמונה משימות חסרות-תאריך ביום הראשון = בדיוק המסך המלחיץ
שאסור לייצר. עם עוגן, אותן שמונה משימות מתפרשׂות על חצי שנה ורואים אחת-שתיים בכל רגע.

## ח.3 מה נזרע — לפי שלב

**עוגנים אפשריים:** תאריך חתימת החוזה · תאריך מסירת המפתח · תאריך תחילת חוזה שכירות.
כולם כבר נאספים באשף.

### קניתי — המפתח טרם נמסר
| משימה | עוגן | הערה |
|---|---|---|
| תשלום מס רכישה | חתימת החוזה | ⚠️ מועד חוקי — ראו ח.4 |
| השלמת ביטוח חיים וביטוח מבנה | לפני ביצוע המשכנתא | לרוב תנאי לביצוע ההלוואה |
| ביצוע ההלוואה מול הבנק | מסירה פחות ~חודש | |
| תשלום למוכר / לקבלן | לוח-התשלומים בחוזה | ⚠️ איננו יודעים אותו — ראו ח.4 |
| בדיקת ליקויים / פרוטוקול מסירה | מסירה פחות ~שבועיים | |
| חיפוש שוכר | מסירה פחות ~חודשיים | |
| הכנת חוזה שכירות | מסירה פחות ~חודש | |
| העברת חשמל, מים, ארנונה וועד על שמך | יום המסירה | |

שמונה משימות, אבל **על פני חודשים** — ובכל רגע נתון גלויות אחת או שתיים.

### יש מפתח, אין שוכר
| משימה | עוגן |
|---|---|
| צילום הדירה ופרסום מודעה | מיד |
| הכנת חוזה שכירות | מיד |
| ביטוח מבנה, אם טרם נעשה | מיד |
| ארנונה על שמך | מיד |

### מושכרת
**לא נזרע דבר.** המנוע הקיים כבר מכסה: אישור שכר-דירה, חידוש-חוזה, פריטים חוזרים.
זריעה נוספת כאן היא כפילות.

## ח.4 🔴 החוק שאסור לעבור: לא ממציאים תאריכים

חלק מהפריטים למעלה נושאים **מועדים חוקיים או חוזיים** — מס רכישה, ולוח-התשלומים
למוכר. **אנחנו לא יודעים אותם**, והם משתנים מעסקה לעסקה.

לכן: לפריטים האלה האפליקציה **תיצור את המשימה ותבקש מהמשתמש לקבוע את התאריך**,
עם ניסוח שאומר במפורש לאמת מול עורך-הדין. היא לא תקבע תאריך בעצמה ולא תציג מספר-ימים
כאילו הוא עובדה.

זה גם הכלל הנכון מבחינת חוויה (היוריסטיקה 2 — התאמה לעולם האמיתי): לא להעמיד פנים
שאנחנו מכירים את החוזה שלו.

**חריג לחוק ח.2:** משימה כזו נזרעת בלי תאריך, ולכן תהיה גלויה מיד. זה מכוון — היא
נזרעת אחת, בשלב הראשון, והיא בדיוק מה שהמשתמש צריך לראות ביום הראשון.

## ח.5 מה קורה כשמסמנים "בוצע"

המנוע כבר מציע לרשום תנועה כספית בסיום משימה. זה מתאים מצוין לחלק מהפריטים:

| משימה | בסיום |
|---|---|
| תשלום מס רכישה · תשלום למוכר | הצעה לרשום **הוצאה** — קיים היום |
| ביצוע ההלוואה | הצעה להשלים את פרטי המשכנתא | *(חיווט חדש — לאישור)* |
| הכנת חוזה שכירות | הצעה לפתוח חוזה | *(חיווט חדש — לאישור)* |
| השאר | סימון בלבד |

## ח.0 🔴 החלק הזה מוחזק בספק (09.09)

הבעלים, 08.09: *"חלוקת המשימות והתהליך שאנחנו רוצים לעבור עם המשתמש רחוק מאוד."*
צודק. **הלימוד עבר ל-[`purchase-process.md`](./purchase-process.md)** — טבלה עם
מקורות, ועם הסיווג שהוא ביקש (תלוי-תאריך · תלוי-בך · תלוי-צד-שלישי).

מה שהתברר, ורלוונטי ישירות לחלק הזה:

- **ח.3 היה קרוב יותר לאמת מהקוד.** בטבלה כאן מס-רכישה מעוגן ב"חתימת החוזה" —
  ואילו `purchaseTasks.ts` שנכתב אחר-כך עיגן **הכול** במסירה והפך את מס-הרכישה
  למשימה חסרת-תאריך. **הקוד סטה מהאפיון, ואף אחד לא שם לב.**
- **שניהם לא ידעו את המספרים:** 30 יום לדיווח, 60 יום לתשלום, **מיום החתימה** —
  תאריך ש-`purchase_date` כבר מחזיק.
- **"לבצע משכנתא" אינו מועד אלא חלון:** ריבית תקפה ~24 יום, שלב בטחונות 10–14 ימי עסקים.
- **חסרים לגמרי:** מדד תשומות הבנייה (ה-15 בחודש), ערבויות חוק מכר, שנת בדק, רישום.

עד שתעבור על `purchase-process.md` — **הטבלאות למטה אינן אמת, הן ההשערה הקודמת.**

## ח.6 מה פתוח כאן

1. **רשימת המשימות עצמה** — האם היא נכונה ומלאה? זו רשימה שנכתבה מהיכרות כללית עם
   תהליך רכישה בישראל, ולא מהעסקה של האח או החבר. **שווה לעבור עליה איתם.**
2. **המרווחים** ("שבועיים לפני", "חודשיים לפני") — ניחושים סבירים, לא ידע. לאישור.
3. **מי מסמן** — האם המשתמש מסמן ידנית בלבד, או שהאפליקציה מסיקה (למשל: נוסף חוזה
   שכירות ⇒ "הכנת חוזה שכירות" נסגרת מעצמה). ההסקה נעימה אבל מסוכנת — היא יכולה
   לסגור משימה שהמשתמש לא סיים.
4. **חלון-התצוגה** (ח.1) — לאשר את המעבר מ"עוד N בעתיד" ל"המשימה הבאה בשמה".

---

# חלק ט׳ — מסך-הבית לפני מסירת המפתח (07.09)

**הכרעת הבעלים שסוגרת את פתוח #2:**
> "צריך לעצב את האפליקציה כשהמשתמש רכש דירה אך היא עדיין לא בבעלותו — שם זה צריך
> להראות יותר **תחזית**, **משימות**, ולעזור לעבור את השלב הזה **ולהכניס ודאות**."

"ודאות" היא ההגדרה המדויקת של התפקיד, והיא גם מה שהופך את המסך הזה לבר-אפיון:
אפשר לבדוק כל אלמנט מולה.

## ט.1 מה המסך מראה להם היום

`expectedNet = max(שכר-דירה, שהתקבל) + הכנסות נוספות − הוצאות קבועות − הוצאות נוספות`
(`HomeScreen.tsx`). לרוכש שטרם קיבל מפתח: אין חוזה ⇒ שכר-דירה 0; מסלולי-המשכנתא
מתחילים בתאריך-המסירה, ו-`monthlyVirtualEntries` חסום-לוח-זמנים ⇒ הוצאות קבועות 0.

**התוצאה: הכותרת הגדולה של אפליקציית-הכסף מציגה "צפי לסוף החודש · ₪0".**

זו לא שגיאה בקוד — הקוד צודק לחלוטין. **החודש הזה באמת שווה אפס עבורו.** הבעיה היא
שהשאלה שגויה: החיים שלו לא מחולקים לחודשים. **הכרטיס מודד בשעון הלא-נכון.**

## ט.2 ההמלצה המבנית: אותו מסך, שעון אחר

לא לבנות מסך-בית שני. **שלושת המקטעים של הבית נשארים במקומם ובמשקלם, ומשנים שעון:**

| מקטע | היום (מחזור חודשי) | לפני מסירה (מסע) |
|---|---|---|
| מרכז-הפעולות | מה דורש אותך החודש | **המשימות של השלב** — כבר עובד, רק צריך שיהיו משימות (חלק ח׳) |
| כרטיס-התזרים | "צפי לסוף החודש" | **כרטיס-היעד** — ראו ט.3 |
| כפתורי-הוספה | הוצאה · משימה | ללא שינוי — יש להם הוצאות אמיתיות בשלב הזה |

שלוש סיבות: עקביות (היוריסטיקה 4) · אין מסך שני לתחזק · וביום המסירה **המקטעים
פשוט חוזרים לומר דברים אחרים**, בלי מעבר-מסכים ובלי הפתעה.

## ט.3 שלוש הוודאויות

הרוכש נושא ארבע שאלות. כל אחת היא אי-ודאות, ולכל אחת יש תשובה שאנחנו יכולים לתת:

| השאלה שבראש | הוודאות שהמסך נותן | מהיכן |
|---|---|---|
| "מתי זה נגמר?" | תאריך המסירה, והמרחק אליו | `key_delivery_date` — כבר נאסף |
| "כמה כבר שמתי?" | ההון והעלויות שכבר נכנסו | שלב-ההשקעה — כבר נאסף |
| "כמה זה יעלה לי כשזה יתחיל?" | **התשלום החודשי הראשון, וממתי** | מנוע-המשכנתא — כבר מחשב |
| "מה עוד צריך לקרות?" | המשימה הבאה בשמה ובתאריכה | חלק ח׳ |

**הכרטיס המרכזי (במקום "צפי לסוף החודש") הוא כרטיס-יעד:** התאריך, המרחק אליו, וכמה
כבר נכנס — ומתחתיו התחזית לחודש הראשון האמיתי.

## ט.4 הדבר האחד ששווה לבקש — והוא פותח את כל התחזית

יש מספר אחד שאנחנו לא יודעים ושהוא שווה הכול: **שכר-הדירה הצפוי.** רוב הרוכשים
יודעים אותו בערך (זו הסיבה שקנו). שדה אחד, מסומן במפורש כ**הערכה**.

מה הוא פותח: **"כשהדירה תושכר, כך ייראה החודש שלך"** — התשלום החודשי מול שכר-הדירה
הצפוי, והמאזן ביניהם. זה בדיוק מנוע-המשכנתא שכבר קיים ומחשב פיצול קרן/ריבית מודע-גרייס.

וזו נקודת-הכוח הייחודית של האפליקציה בשלב הזה: **אף אחד אחר לא יכול להגיד לו את
זה.** לא עורך-הדין, לא הבנק, לא גיליון-אקסל. זו התשובה לשאלה שהכי מטרידה אותו,
מנתונים שהוא כבר נתן.

## ט.5 החוק: ודאות אינה אופטימיות

אם המאזן החודשי הצפוי שלילי — **אומרים אותו.** ודאות פירושה **לדעת**, לא להירגע.
בכל מקום שבו נצטרך לבחור בין "מרגיע" ל"מדויק" — מדויק מנצח. זו אפליקציית-כסף
שמשפחה סומכת עליה, והמספר חייב להיות בר-סמיכה.

מאותה סיבה, כל מספר שנשען על מה שהוזן ידנית (מסלול מאישור-עקרוני, שכר-דירה משוער)
מוצג כ**הערכה** ולא כעובדה.

## ט.6 מה מכוון שלא נבקש בגרסה הראשונה

**את לוח-התשלומים למוכר/לקבלן.** הוא התשובה המלאה ל"מה נשאר לשלם" — ולכן מפתה —
אבל הוא חמש-שש שורות של תאריכים וסכומים. לבקש אותו באשף סותר ישירות את שתי ההכרעות
שקיבלנו (האשף לא שער · רק מה שבאמת צריך).

**במקום זה:** משימה שמזמינה להזין אותו מתוך האפליקציה, כשהמשתמש רוצה. אם יתברר שרבים
ממלאים — נשקול לקדם אותו. זו הדרך הזולה לגלות אם הוא באמת נחוץ.

## ט.7 מה פתוח כאן

1. **המראה** — הרפרנסים שאתה אוסף. שים לב שתפקידם התכווץ ונעשה ממוקד: **לא** מה
   המסך אומר (ט.3 קובע) אלא **איך הוא נראה** — במיוחד כרטיס-היעד.
2. **שדה שכר-הדירה הצפוי** (ט.4) — מאשר להוסיף? זה שדה אחד שפותח את כל התחזית.
3. **מה קורה ביום המסירה** — האם יש רגע-מעבר מפורש ("המפתח שלך"), או שהמסך פשוט
   מתחלף בשקט. ההמלצה: רגע מפורש, כי לפי חוק השיא-והסוף זה נקודת-שיא אמיתית.

---

# חלק י׳ — תוכנית-הבנייה (07.09)

הבעלים: "איך נכון לנו לבנות את זה? אני רוצה שנעשה את זה טוב, וזו משימה גדולה —
כמו רבע אפליקציה."

זה נכון, ולכן **סדר-הבנייה חשוב לא פחות מהתוכן.** להלן ההיגיון, ואחריו הפרוסות.

## י.1 שלושת הכללים שמנחים את הסדר

**1. בונים את היעד לפני הדרך.**
האינסטינקט הוא להתחיל מהאשף — הוא היה נקודת-הפתיחה של כל הדיון. **זו טעות.**
תפקיד האשף הוא להביא אדם למסך שעובד; אם נבנה אותו ראשון, הוא יביא אותו למסך של
היום. בונים קודם את מסך-הבית של טרם-מסירה, ורק כשהוא חי — בונים את הדרך אליו.

**2. אפשר לבדוק את הלב מוקדם, כי המצב נגזר.**
"טרם-מסירה" נגזר מתאריך-מסירה עתידי (חלק ו׳), בלי שום שינוי באשף. כלומר **ברגע
שהמסך קיים אפשר לבדוק אותו** — מזינים תאריך-מסירה עתידי בחשבון-בדיקות ורואים.
לא צריך לחכות לאשף כדי לדעת אם הדבר הזה שווה משהו.

**3. כל פרוסה עולה ל-staging ונבדקת. אין ענף ארוך.**
שינוי בגודל "רבע אפליקציה" שיושב שבועות בענף צדדי הוא הדרך הבטוחה לשבור את מה
שעובד. כל פרוסה למטה עומדת בפני עצמה, נדחפת, ונבדקת באפליקציית-הבדיקות.

## י.2 הפרוסות, לפי הסדר

| # | פרוסה | למה כאן | נראה למשתמש? |
|---|---|---|---|
| 1 | **מודול-המצב** — פונקציה טהורה + בדיקות: טרם-מסירה / ברשותי / מושכרת | יסוד לכל השאר. אפס סיכון | לא |
| 2 | **שכר-דירה צפוי** — עמודה, שדה, וניתן-לעריכה | פותח את התחזית של פרוסה 3 | שדה חדש |
| 3 | **מסך-הבית של טרם-מסירה** — כרטיס-היעד + המשימה הבאה בשמה | **הלב.** בונים ובודקים אותו לפני הכול | ✅ הגדול |
| 4 | **זריעת המשימות** (חלק ח׳) | ממלא את מרכז-הפעולות שפרוסה 3 מציגה | ✅ |
| 5 | **אירוע יום-המסירה** | נקודת-שיא; אפשרי רק כששני הצדדים קיימים | ✅ |
| 6 | **האשף** — שאלת "איפה אתה" + מסלול דינמי + ניסוח | אחרון. עכשיו ידוע בוודאות לאן הוא מוביל | ✅ הגדול |
| 7 | **מעבר-ניסוח** — פתיחה, סיום, מצבי-ריק לפי מצב | ליטוש שנשען על הכול | ✅ |

**נקודת-עצירה מתוכננת בין 3 ל-4:** אחרי שהלב חי, לעצור ולהסתכל. אם מסך-הבית של
טרם-מסירה לא מרגיש שווה — עדיף לגלות שם, לפני שנבנו עליו עוד ארבע פרוסות.

## י.3 🔴 מיגרציה אחת — ולא יותר

פרוסה 2 דורשת עמודה חדשה: `properties.expected_monthly_rent`. אין לה תחליף —
בדקתי, אין בטבלה שדה שאפשר לגייס לזה בכנות (`estimated_value` הוא שווי הנכס).

- **מספר:** 049 (האחרון בריפו הוא 048)
- **מה:** עמודה אחת, מותרת-ריק, בלי backfill ובלי נגיעה בנתונים קיימים
- **מי מריץ:** אתה, שורה אחת. אתן אותה מוכנה-להדבקה כשנגיע
- **כל שאר הפרוסות — אפס שינויי-סכימה.** המצב נגזר, המשימות משתמשות בטבלה קיימת

## י.4 שכר-הדירה הצפוי הוא כבר הסימולטור

הבעלים: "שיהיה ניתן לשינוי, כי זה משנה הרבה."

נכון, ולכן **מקומו על הכרטיס עצמו, לא בטופס נסתר** — נוגעים במספר, והמאזן החודשי
מתחתיו זז. קלט לצד הפלט שלו.

וזה בדיוק **הגרסה הראשונה של הסימולטור** שביקשת לשלב "על סף רכישה": מנוף אחד,
תוצאה מיידית, על נתונים אמיתיים. כשנאפיין את הסימולטור המלא — זו נקודת-הפתיחה שלו,
לא התחלה מאפס.

## י.5 מה יכול להיכשל, ומה מגן

| סיכון | ההגנה |
|---|---|
| **משתמשים קיימים נשברים** — הסיכון החמור ביותר | המצב נגזר, וברירת-המחדל היא "ברשותך". מי שלא מילא תאריך-מסירה עתידי לא רואה שום שינוי. נבדק בפרוסה 1, לפני שנבנה משהו מעליה |
| **בונים את הלב ומתגלה ריק** | נקודת-העצירה בין 3 ל-4 |
| **רשימת-המשימות מנוחשת** | לאמת מול האח והחבר **לפני** פרוסה 4, לא אחריה |
| **זחילה לתוך הסימולטור** | הוא שלב נפרד. פרוסה 2 היא המנוף היחיד שנכנס עכשיו |
| **האשף שובר את מי שכבר עובד** | הוא אחרון, אחרי שהכול מוכח |

## י.6 מה פתוח כאן
- **הרפרנסים** — נדרשים לפני פרוסה 3, ורק לצורת כרטיס-היעד.
- **אימות רשימת-המשימות** מול האח והחבר — לפני פרוסה 4.

## י.7 איפה נבנה, ואיך נבדק

**המסלול:** ענף-העבודה → `staging` → אפליקציית-הבדיקות
(`staging.apartment-6s4.pages.dev`, סמל נפרד, באנר כתום, מספר-גרסה בראש המסך) →
בדיקת-בעלים → "פרסם לכולם" → `main` → ייצור.

**⚠️ מלכוד שחשוב לדעת מראש: יש בסיס-נתונים אחד.**
`deploy-staging.yml` ו-`deploy.yml` משתמשים באותו `VITE_SUPABASE_URL` בדיוק. אין
מסד-נתונים נפרד לבדיקות — שתי הסביבות הן שני *לקוחות* מול אותו פרויקט Supabase.
שלוש השלכות:

1. **מיגרציה 049 נוגעת בייצור ברגע שמריצים אותה**, גם לפני שהקוד עולה. במקרה שלנו
   זה בטוח לחלוטין — עמודה חדשה מותרת-ריק שאף קוד בייצור לא קורא — אבל זו עובדה
   שצריך להכיר, לא להתגלות.
2. **פונקציות-שרת משותפות** ונפרסות רק מ-`main`. כל שינוי-שרת מגיע לייצור. לכן
   פרוסה 4 (זריעת משימות) תיבנה בצד-הלקוח ולא בפונקציה — כך היא נשארת בבדיקות עד
   שנחליט לפרסם.
3. **חשבון-בדיקות הוא חשבון אמיתי** באותו מסד, מבודד ב-RLS (`owner_id = auth.uid()`).
   בטוח, אבל אמיתי.

**איך הבעלים יבדוק את המצב החדש — הוא לא יכול בחשבון שלו.**
החשבון שלו הוא דירה מושכרת, ולכן לא יראה את מסך טרם-המסירה בלי לשבור נתונים
אמיתיים. צריך **חשבון-בדיקות ייעודי** שבו מולא תאריך-מסירה עתידי. בחשבון כזה גם
"חזרה לאונבורדינג" (בתפריט-החשבון, לחשבונות-ניהול) זמין לריצות חוזרות בלי מחיקה.

**מה נבדק לפניו, כדי שלא יגלה תקלות:** לכל פרוסה — 218 בדיקות-היחידה, בנייה
וטיפוסים, ואימות-חזותי חי דרך רתמת-Playwright (הרצת ה-`dist` הבנוי עם הזרקת סשן).
כלומר הוא מקבל צילומים של המסך עובד, ושופט רק את **שיקול-הדעת** — האם זה מרגיש
נכון — ולא האם זה שבור.

---

# חלק י״א — יומן-הבנייה

## פרוסות 0–1 · 07.09 · v1.10.0 — נדחף ל-staging

**פרוסה 1 — מודול-המצב** (`src/lib/stage.ts`)
שתי פונקציות טהורות: `possession()` (מחזיקים במפתח?) ו-`leaseStatus()`
(מושכרת / חתום-וטרם-התחיל / מחפש / מעולם לא). 19 בדיקות, ובהן בדיקה מפורשת שכל
משתמש קיים — תאריך ריק או שעבר — נקרא `in_hand` ולא זז.

**פרוסה 0 — טוען-התרחישים** (`src/lib/scenarios.ts` + מקטע בהגדרות)
שלושה תרחישים על אותה דירה: תהליך רכישה · יש מפתח, אין שוכר · מושכרת. הכפתור
**טוען נתונים**, ולא מכריח מצב — כך שהמסך נגזר דרך אותו נתיב שירוץ אצל משתמש
אמיתי, ולא דרך נתיב-בדיקה מקביל שיכול להיראות תקין בזמן שהאמיתי שבור.

**שער:** `(isStaging || DEV) && (אדמין || מנהל)`. לא נראה בייצור לעולם — וזו גם
ההפרדה שביקש הבעלים: "פרסם לכולם" על משהו אחר לא יכול לגרור את זה פנימה.

### איך אומת
- **237 בדיקות-יחידה** (25 חדשות), בנייה וטיפוסים נקיים, אפס בעיות-לינט חדשות.
- **בדיקה חיה מול המסד האמיתי** בחשבון `dev@test.local`: הורצו שני התרחישים
  מקצה-לקצה ואומת שכל שורה נכתבה — נכס, שתי מסלולי-משכנתא שמתחילים בתאריך-המסירה,
  שלוש עלויות, ובתרחיש המושכר גם חוזה ופריט-שכ״ד מקושר. **הסכימה קיבלה הכול.** זה
  החלק שבדיקות-יחידה אינן יכולות להוכיח.
- **מה לא אומת כאן:** התצוגה עצמה. הדפדפן בסביבת-הענן לא מצליח לפתוח מנהרת-TLS
  ל-Supabase דרך הפרוקסי (`ERR_CONNECTION_RESET`), ולכן צילומי-מסך חיים אינם
  אפשריים מכאן. **האימות החזותי של הפרוסה הזו הוא הלחיצה שלך על הכפתור בסטגינג** —
  וזה גם בדיוק מה שהיא נועדה לאפשר לכל הפרוסות הבאות.

### הבא בתור
פרוסה 2 — `properties.expected_monthly_rent` (מיגרציה 049) והשדה הניתן-לעריכה.

## פרוסה 3 · 07.09 · v1.11.0 — נדחף ל-staging

**סדר שונה מהתוכנית, במכוון.** פרוסה 2 חסומה עד שמיגרציה 049 תרוץ (אין לי טוקן-ניהול
בסביבה), אז דילגתי עליה. הלב אינו תלוי בעמודה: ספירה, הון שנכנס, ותשלום ראשון —
כולם מנתונים קיימים. שכר-הדירה הצפוי יתווסף כשורה נוספת בכרטיס כשהעמודה תיווצר.

**`PreKeyCard`** מחליף את "צפי לסוף החודש" כשהמפתח טרם נמסר, ועונה על שלוש
הוודאויות מחלק ט׳:
- **מתי זה נגמר** — ספירה מילולית מקורבת ("בעוד כ-7 חודשים") עם **התאריך המדויק
  מתחתיה**, ופס-התקדמות מחתימת-החוזה עד המסירה — הדבר היחיד שזז בזמן ההמתנה.
- **כמה כבר שמתי** — סך ההון והעלויות מ-`useInvestmentData`.
- **כמה זה יעלה כשזה יתחיל** — התשלום החודשי הראשון, מ-`monthlyVirtualEntries`:
  **אותו מנוע** שהתזרים ומסך-החודש קוראים לו, בשאלה על חודש-המסירה במקום על החודש
  הנוכחי. לכן הוא לא יכול לסטות מהמספרים שמוצגים במקום אחר.

הרכיב נטען **רק** בחשבון טרם-מסירה, ולכן שליפת-עלויות-ההשקעה שהוא צריך לא רצה
במסך-הבית של אף אחד אחר.

### איך אומת
243 בדיקות (6 חדשות), בנייה וטיפוסים נקיים, אפס לינט חדש (2 השגיאות ב-`HomeScreen`
קיימות מלפני — אומת ב-stash). בין הבדיקות אחת שמוכיחה את הטענה של הכרטיס: על נתוני
התרחיש יש 751,000 ₪ שכבר נכנסו ותשלום ראשון חמש-ספרתי אמיתי, **ובאותו חודש נוכחי
המנוע מחזיר אפס שורות** — כלומר ה-₪0 שראינו היה נכון, והשאלה היא שהייתה שגויה.

**אימות חזותי — עליך:** הגדרות ← תרחישי בדיקה ← "תהליך רכישה", ואז מסך-הבית.

## פרוסה 4 · 07.09 · v1.12.0 — נדחף ל-staging

**המשימות נזרעות — אבל בהזמנה, לא מעצמן.**
`src/lib/purchaseTasks.ts` מחזיק את התוכנית, ו-`PreKeyTaskInvite` מציע אותה
בכרטיס אחד מתחת לכרטיס-היעד: "7 דברים לסדר עד המסירה · הוסיפו למשימות". הקשה אחת.

**למה הזמנה ולא זריעה אוטומטית:** להפיל שבע שורות לחשבון של מישהו בלי לשאול היא
אותה טעות בדיוק כמו אשף שדורש 30 שדות מראש. ההצעה מופיעה **פעם אחת** — ברגע
שקיימת משימה כלשהי מהתוכנית (פתוחה או שבוצעה) היא נעלמת, כך שמי שעובר על הרשימה
לא רואה אותה חוזרת.

**התוכנית:** שש משימות מעוגנות לתאריך-המסירה (חיפוש שוכר −60 · ביטוח −45 · ביצוע
המשכנתא −30 · חוזה שכירות −30 · בדיקת ליקויים −14 · העברת חשבונות ביום המסירה),
ואחת בלי תאריך — **"לקבוע את מועדי התשלום מהחוזה"**, שמכסה מס-רכישה ותשלומים למוכר.
זה החריג המכוון מחלק ח.4: מועדים חוקיים וחוזיים שאיננו יודעים, ולכן מבקשים ולא מנחשים.

**עיגון שלא מייצר איחורים:** כל עוגן שכבר עבר נצמד להיום. מי שנרשם שלושה שבועות
לפני המסירה לא מקבל חמש שורות אדומות על עבודה שמעולם לא התבקש לעשות.

**נלוות:**
- **המשימה הבאה בשמה** — `nextScheduledTask()` היה כתוב מאז שהבעלים אמר "אני לא
  רואה את זה", ומעולם לא חובר. מסך-הבית הציג רק מונה. עכשיו: "הבא: בדיקת ליקויים ·
  24.3 · ועוד 5". חל על כולם, לא רק על טרם-מסירה.
- **בלי מסגרת-תזרים לפני מסירה** (החלטת-בעלים) — קישור "פירוט" הוסר; אין זרימה
  חודשית, והקישור הצביע על מסך ריק.
- **תרחיש רביעי: "מסירה בעוד שבועיים"** — הקצה הצפוף, שבו רוב הרשימה מתכנסת ליום
  אחד. זה המקרה שכדאי להסתכל בו כדי לשפוט את כלל "לא להציף".

### איך אומת
251 בדיקות (8 חדשות), בנייה וטיפוסים נקיים, אפס לינט חדש. הבדיקות מגינות על שני
הכללים במפורש: **בדיוק משימה אחת בלי תאריך**, ו**אף משימה לא נולדת באיחור**.
בנוסף — הרצה חיה מול המסד: תרחיש "מסירה בעוד שבועיים" נזרע, ושבע המשימות נכתבו
בפועל ל-`tasks`. הסכימה קיבלה את כולן.

## פרוסות 2 + 5 + סבב-סקירה · 07.09 בלילה · v1.14.x — ריצה עצמאית

הבעלים הלך לישון וביקש לרוץ קדימה, כולל סקירות. להלן מה נעשה ומה נמצא.

### פרוסה 2 — שכר-דירה צפוי (התברר שאינה חסומה)
`usePropertyData` עושה `select('*')`, ולכן **עמודה שטרם קיימת פשוט חוזרת חסרה במקום
לזרוק שגיאה.** הכרטיס מזהה תמיכה עם `'expected_monthly_rent' in property`, מסתיר את
השדה עד אז, ומדליק אותו **בלי פריסה** ברגע שמיגרציה 049 תרוץ. הכתיבה נגישה רק
כשהשדה מוצג, ולכן לא יכולה לפגוש עמודה חסרה.

**אומת חי:** נשלף `select('*')` מהמסד — המפתח אכן חסר כרגע, `key_delivery_date` קיים.
כלומר ההסתרה עובדת מהסיבה הנכונה, לא במקרה.

השדה יושב על הכרטיס ליד התוצאה שלו, ומתחתיו **"המאזן החודשי אז"** = צפי פחות התשלום
הראשון. שמירה אופטימית עם החזרה-לאחור בכישלון. המאזן מוצג חתום ואמיתי — שלילי נאמר,
לא מרוכך.

### פרוסה 5 — רגע המסירה
מכיוון שהמצב נגזר מהתאריך, יום-המסירה היה עובר **בשקט**: כרטיס-הספירה נעלם, מחזור
חודשי תופס את מקומו, ואיש לא מסביר למה האפליקציה הפכה לכלי אחר. זו גם נקודת-השיא
היחידה במסע, ולפי חוק השיא-והסוף היא מה שייזכר מכל חודשי ההמתנה.

כרטיס, לא חלון-קופץ — אדם שאוסף מפתחות לא צריך שיפריעו לו. מברך, אומר מה השתנה,
מציע חוזה-שכירות כשאין, ונסגר. הסגירה נשמרת ב-localStorage לפי נכס+תאריך: פר-מכשיר,
וזה נכון לברכה — עדיף מלשמור מצב-ממשק במסד-הכסף. זמין 14 יום אחרי התאריך, למי שלא
פתח את האפליקציה באותו יום.

תרחיש "יש מפתח, אין שוכר" נמסר עכשיו **אתמול** במקום לפני חודש, כדי שתרחיש אחד יראה
גם את הרגע וגם את המצב היציב שמאחוריו.

### סבב-הסקירה — חמישה ממצאים על הקוד שנכתב היום
| # | ממצא | סטטוס |
|---|---|---|
| 1 | חישוב-תאריכים ב-`purchaseTasks` עבר דרך `Date.UTC` + `toISOString().slice` — עקבי בפני עצמו, אבל בדיוק הדפוס שהריפו אוסר בגלל אזור-הזמן | **תוקן** — עוזרי-התאריך המקומיים |
| 2 | למשימה יש רק כותרת, **אין שדה-הערות** — ולכן ההנחיה שהאפיון הבטיח ("לאמת מול עו״ד") לא הייתה לה איפה לחיות, והמשימה לא אמרה מה היא מכסה | **תוקן** — הכותרת מנסחת את שניהם: מס רכישה ותשלומים למוכר |
| 3 | הוספת הצ׳קליסט לא אישרה כלום — הכרטיס פשוט נעלם. שבע שורות שמופיעות במקום אחר אינן משוב | **תוקן** — הבזק עם המספר |
| 4 | **כל החוויה תלויה בשדה אופציונלי אחד** (תאריך מסירה) שיושב עשירי בטופס ארוך, ואיש לא אומר שהוא עושה משהו. מי שלא ימלא אותו — וזה רוב האנשים — לעולם לא יראה דבר מכל זה | **תוקן** — שני המקומות שמחזיקים את השדה מסבירים מה הוא עושה |
| 5 | "כבר הושקע" טוען יותר ממה שידוע: `self_equity` הוא ההון **בעסקה**, ולרוכש באמצע לוח-תשלומים חלק ממנו עוד לא יצא מהחשבון | **תוקן** — "הון עצמי ועלויות", כמו במסך ההון |

### מה לא נגעתי בו בכוונה
- **פרוסה 6 (האשף)** — היא אחרונה בתוכנית, והתנאי שלה הוא שהלב יוכח. הבעלים עוד לא
  הסתכל, ו"קיצור הליבה" מוקפא בבקשתו. בנייה שם עכשיו הייתה סותרת את שתי ההחלטות.
- **מצב "חוזה חתום שטרם התחיל" לפני מסירה** — קיים בלוגיקה (`leaseStatus === 'signed'`)
  אך אינו מוצג בבית. פער ידוע, קטן, ומחכה לתורו.

### מצב הבדיקות
253 בדיקות ירוקות · בנייה וטיפוסים נקיים · אפס בעיות-לינט חדשות (הקיימות אומתו
ב-stash). אימות חי מול המסד: שני התרחישים נכתבו, שבע המשימות התקבלו, וזיהוי-העמודה
אושר מהסכימה עצמה.

## ריצת-הלילה · 08→09.09 · v1.17.4 — נדחף ל-staging

**מה הוגדר מראש, יחד:** `docs/specs/night-run-2026-09-08.md`. הכלל שנקבע לפני שנגעתי
בקוד: *לא בונים לפני שמבינים; שופטים מסך מצילום ולא מקריאת קוד; מלל באורך תווית.*

### מה נמצא ותוקן — שלוש אמירות לא נכונות, כולן על מסך שאיש לא הסתכל בו
| מסך | מה נאמר | מה נכון | איך אומת |
|---|---|---|---|
| הון | "+2,417 ₪ **לבעלות החודש**" · "מכל תשלום חודשי של 7,092 ₪" | המשכנתא מתחילה בעוד 7 חודשים. **וגם בעל דירה בגרייס שומע היום את אותו משפט** | `wealth--prekey-before-fix` מול `wealth--prekey` |
| תזרים | פס חצי-ירוק חצי-אדום ו-0 ₪ בירוק-הכנסה | לא זזה אגורה. `inPct` נפל ל-50 כברירת-מחדל **מעל לא-כלום** | `finances--prekey-before-fix` מול `finances--prekey` |
| הנכס | "אין חוזה שכירות" בכתום-אזהרה, והמסך נפתח על טאב החוזה | לפני מסירה זה המצב **היחיד** האפשרי, לא תקלה | `property--prekey-before-fix` מול `property--prekey` |

**התיקון בהון הוא מבני, לא ניסוחי:** `currentSplitInfo` מחזיר עכשיו גם את החודש
שאליו נפל וגם האם הוא ההווה. מי שמנסח בלשון הווה חייב לבדוק את הדגל: הכותרת שותקת,
המאיץ אומר "· מאפריל 2027", וכרטיס הרווח החודשי לא נפתח על חודש שלא משלמים בו.

### בדיקות עיצוב — `e2e/design-checks.spec.ts`
ארבעה מסכים × שני שלבים × שני מצבי-תצוגה. כשל קשה: גלישה אופקית, כיוון, רקע שקוף.
לשיפוט: מטרת-מגע ומידת-טקסט → `docs/audit/design-checks.md`.

מטרת-מגע נמדדת כ**שטח-מגע אמיתי** — מגששים 21px מהמרכז לארבעה כיוונים ושואלים את
הדפדפן מה נמצא שם. ולכן ריפוד נספר, צפיפות נכשלת, ועיגול בן 44px עובר.
**שלוש גרסאות ראשונות היו שגויות** (אלמנטים מתחת לקיפול, שורות בתוך אקורדיון סגור,
פינות של עיגול) — 72 "ממצאים" הפכו ל-4 אמיתיים. הכלי חייב להיות נכון לפני שסומכים
עליו. הממצא האמיתי היחיד תוקן: שורות-הסינון בתזרים עמדו על 23px, חצי מהרצפה.

### מה **לא** נעשה, במכוון
לא נגעתי ב-`purchaseTasks.ts` ולא בחרתי צורת-הצגה. המחקר העלה שהעוגן עצמו שגוי,
ולבנות צ׳קליסט על עוגן שגוי זה להאיץ לכיוון הלא-נכון.

---

# חלק י״ב — שאר המסכים לפני מסירה (09.09)

הבעלים, 08.09: *"וגם, מה עם שאר המסכים, התזרים, ההון והנכס."* צודק — כל העבודה עד
כה נגעה במסך-הבית בלבד. להלן מה שלושת האחרים עושים לרוכש שטרם קיבל מפתח, **מהצילומים**
(`docs/audit/evidence/*--prekey--*` — התיקייה מוחרגת מגיט כתוצרי-ביקורת, כך
שהצילומים הם הכלי ולא המסמך; מה שנפתח לבדיקה הוא staging).

## י״ב.1 תזרים — ריק, אבל לא נכון שהוא ריק

תוקן הלילה: הפס והאפסים. **מה שנשאר פתוח גדול יותר:**

> מסך ההון אומר "הושקע נטו 751,000 ₪". מסך התזרים אומר "אין תנועות בחודש זה".
> **שני המסכים חלוקים על השאלה אם יצא כסף.**

הסיבה: הון-עצמי ועלויות-רכישה יושבים ב-`investment_costs`, ולא ב-`transactions`.
זו הפרדה נכונה מבחינה חשבונאית — ושקופה למשתמש, שרואה שני מסכים סותרים.

**החלטה שצריכה אותך:** האם תשלומי הרכישה הם "תזרים"? שלוש אפשרויות:
1. לא. התזרים הוא המחזור החודשי בלבד — ואז לפני מסירה המסך אומר במפורש
   *"התזרים מתחיל בקבלת המפתח"* במקום להיראות שבור.
2. כן, כשורות-תחזית מלוח-התשלומים (שאיננו יודעים — ראו `purchase-process.md`).
3. מסך שלישי לתקופת הרכישה. יקר.

**המלצתי: 1.** זול, כן, ומסיר את הסתירה בלי מודל חדש.

## י״ב.2 הון — המסך הכי שימושי לפני מסירה, ועדיין מדבר על עבר שלא קרה

אחרי התיקון הוא כבר לא משקר על החודש. שלושה דברים עדיין מדויקים-למראית-עין:

- **"בבעלותך 38% מהנכס"** — המשכנתא טרם נלקחה, ההון טרם שולם במלואו. זה הציור של
  **יום המסירה**, מוצג כהווה.
- **"נותר לבעלות · בנקים 1,150,000 ₪"** — חוב שעוד לא נוצר.
- **"הון עצמי ועלויות רכישה 751,000 ₪"** — לפי המחקר, בעסקה טיפוסית 5%–15% משולמים
  בחתימה ו-15%–20% **ביום המסירה**. כלומר הסכום המלא כמעט בוודאות עוד לא יצא.

**שאלה אחת פותחת את כל השלושה:** "כמה כבר שילמת עד היום?" — ואז ההון מציג *"שולם
X מתוך Y"* במקום להניח שהכול שולם. זו גם בדיוק השאלה שהתשובה לה מייצרת את התחזית
בכרטיס טרם-המסירה. **פריט אחד, שלושה מסכים.**

## י״ב.3 הנכס — עכשיו נכון, ועדיין ריק

תוקן: השבב הכתום והטאב הפותח. מה שנשאר: הטאב שאליו נוחתים (משימות) אומר
*"אין משימות פתוחות — הכול תחת שליטה"* לאדם שיש לו שבעה חודשים של עבודה לפניו.
**ההרגעה הזו נכונה למי שיש לו מחזור חודשי מסודר, ולא נכונה כאן.**

היא תיפתר מאליה ברגע שנדע מה המשימות באמת — ולכן לא נגעתי בה הלילה.

## י״ב.4 מה שחוזר בשלושתם

1. **אין מסך שאומר "השלב הזה מתחיל בקבלת המפתח".** כל אחד מהם פשוט נראה מקולקל.
2. **המידע היחיד שחסר לשלושתם הוא אותו מידע** — מה כבר שולם, ומה לוח התשלומים.
3. **הבית לבדו לא מספיק.** אדם שיפתח את "הון" ביום הראשון יקבל תמונה שנכונה
   ליום המסירה ולא להיום, ולא תהיה לו שום דרך לדעת.

## י״ב.5 ובדרך — המצב שלך, מצולם לראשונה (09.09)

שלושת העמודים במצב **מושכרת** מעולם לא צולמו (`*--leased--*`). שני דברים עלו:

**(א) אותה סתירה בדיוק, במסך שני.** מסך התזרים מציג כותרת אדומה גדולה
**"מאזן החודש · ‎−1,987 ₪"**, ולידה, קטן ואפור: *"מאזן חיסכון בפועל ‎+563 ₪"*.

> המספר הכן כבר מחושב, כבר מוצג — ומוצג קטן ואפור ליד מספר אדום וגדול שסותר אותו.

זה בדיוק ממצא 1 מסיור-המוחות, במסך אחר. **כלומר זו לא שאלה של מסך-הבית, אלא החלטה
אחת שחלה על שני מסכים:** מהו המספר הראשי — התזרים או החיסכון. (פתוח #4.)

**(ב) תוקן אגב:** אפשר היה להזין מסירה **לפני** החתימה, וכותרת מסך-הנכס הציגה את
הצמד ההפוך כעובדה. שדה המסירה חסום עכשיו לתאריך החתימה ומעלה.
