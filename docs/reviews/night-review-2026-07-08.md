# סקירת-לילה 2026-07-08 — דיוק עיצובי/חוויה + פונקציונליות

ריצה אוטונומית. סקירה רב-סוכנית קריאה-בלבד על שני צירים (עיצוב/UX + פונקציונליות/בדיקות),
עם אימות-אדברסרי לכל ממצא, ואז החלת ה-🟢 הבטוחים. **סייג חשוב:** הריצה נקטעה באמצע —
**נגמרה מכסת-ההוצאה החודשית של החשבון** (14 סוכני-אימות נכשלו על "monthly spend limit").
לכן חלק מהממצאים נשארו **לא-מאומתים**, וכל העבודה מכאן בוצעה **בחסכנות, בלי עוד workflows**.

## מה נדחף ל-main (🟢 — 27 ממצאים מאומתים, 4 קומיטים)
`ae45ecf` · `e51314c` · `0abc9df` (+ `cb163cc..` הבסיס). בכל קומיט: tsc נקי · 79 בדיקות · בנייה עוברת.

**RTL / דו-כיווניות:** `.hs-flow-amt.income` ו-`.finv-summary-net` קובעו לכיוון/יישור נכון ·
כרטיס-פעולה "נפתר" מחליק לקצה-הנגרר · דלתא ב-OwnershipScore עברה ל-`formatSignedCurrency`
(סימן RTL תקין) במקום `+` ידני.

**נוסח:** ספירת-חידוש מטפלת ב-0/1 ימים ("נגמר היום/מחר") · חוזה פעיל ביומו האחרון מציג
"מסתיים היום" ולא "פג תוקף" · כותרת-הבית לא טוענת "פעולות מחכות" כשאין נכס מוגדר.

**תאריכים (כלל-הבית):** FinancingStructure + CalendarPopover מנתחים ISO דרך `parseLocalISO`,
לא `new Date()` · InsuranceStep מציג `formatDate`, לא ISO גולמי.

**נגישות / מגע:** כפתור-סגירה של BottomSheet ו-ניווט-לוח-השנה הוגדלו ל-44px · כפתורי
אישור-מחיקה הורמו ל-target תקין (מגודר; `.btn-xs` המשותף לא נגע) · סולם-הבעלות המעטר סומן
`aria-hidden` (המקרא כבר מוסר את הפירוט) · מלכודת-Tab ל-Modal (כמו BottomSheet) · Esc-לסגירה
+ `aria-modal` לעורך-ההון (WealthHub) ולדיאלוגי "חסרים פרטים" (Mortgage/Loans).

**התנהגות (כמו רכיבי-אח):** קיפול כרטיס-ביטוח בעריכה **שומר** את השינויים (כמו Mortgage/Loans)
במקום לזרוק · פתיחת הלוואה-חודשית לעריכה מסנכרנת את מתג-הגרייס.

**ביצועים (זהה-התנהגות):** 6 הוקים (property/mortgage/insurance/loans/documents/investment)
תלויים ב-`[user?.id, cacheKey]` במקום `[user, ...]` — רענון-טוקן כבר לא גורם שליפה-מחדש
מיותרת (כמו transactions/tasks).

**קוד-מת:** `.wlth-empty`/`.wlth-mix`/`li.muted` הוסרו · ענף follow-up המת "הפקדת צ׳ק"
(+ הבדיקה היתומה).

**באג-a11y שתוקן:** צ׳יפ הלוואת-הבלון (InvestmentStep) — גוף-העריכה וכפתור-המחיקה הם עכשיו
`<button>` אמיתיים (נגישים-מקלדת) במקום span-onClick בתוך `role="button"` (קינון לא-חוקי).

## 🔴 ממצאים שדורשים אותך — לא הוחלו (מאומתים-אמת אך כסף/התנהגות/עמימות)
_מהסקירה. כל אחד מיקום + סיבה. לא בטוחים להחלה עיוורת — כמה דורשים החלטת-מוצר או בדיקה-חיה._

**גבוה:**
- **push.ts:111** — כיבוי-התראות בהגדרות **מתבטל בשקט בפתיחה הבאה**: `disablePush` מוחק שורה
  ורושם unsubscribe, אך לא שומר כוונת-opt-out, וההרשאה בדפדפן נשארת `granted` → הפעלה-מחדש
  ברקע. דורש דגל-כוונה מתמשך. (התנהגות)

**בינוני:**
- **useInvestmentData.ts:57** — בולע שגיאות-שליפה של tracks/contracts/loans → מציג ריבית/שכ״ד
  שגויים בלי שגיאה. אותו דפוס "כשל-שקט" שתוקן במקומות אחרים, אך כאן נוגע-כסף → לך. (אפשר להחיל
  בזהירות: throw על השגיאות במקום `?? []`.)
- **useMonthlyGeneration.ts:6** — מפתח `monthly_generation` ב-localStorage **לא ממותג-משתמש**:
  משתמש שני על מכשיר משותף לא מקבל גנרציה-חודשית. (מתחבר ל-R15 של דליפת-מכשיר-משותף)
- **LiabilitiesV2.tsx:364/411/…** — מחיקות במסך-ההתחייבויות **בלי אישור**, בניגוד לכל מחיקה
  אחרת (חוזה/פוליסה/מסלול-אונבורדינג מבקשים אישור). (התנהגות)
- **RentalStep.tsx:58** — תאריך-סיום-שכירות מחושב-אוטומטית פעם אחת בלבד; הזזת ההתחלה לאחרי
  הסיום-האוטומטי מפילה בשקט את כל השכירות ב-finish. (התנהגות)

**נמוך:**
- **ExpenseSheet.tsx:201** — ספרות ה-numpad מוצגות מראה (1-2-3 מימין-לשמאל) מול מקלדת-טלפון
  תקנית. (RTL — מגודר כי `direction:ltr` על ה-numpad יכול להשפיע על מסלול-ההחלקה; דורש בדיקה)
- **finances-v2.css:139** — מגירת-העריכה נכנסת מהשמאל-הפיזי באפליקציית-RTL. (RTL — עוגן `left:0`)
- **WelcomeStep.tsx:31** — הבטחה "אפשר לדלג בכל שלב" אך צעד-הרכישה חסר כפתור-דילוג/סיום. (מוצר)
- **index.css:4891 / 4937** — צבעי-פסי-הון (dash/wealth) הם hex-קשיח, לא tokens → לא מסתגלים
  ל-dark mode. (עיצוב; ראה גם `--*-soft` למטה)
- **useMonthlyGeneration.ts:189** — insert של משימת-חידוש בלי מגן-אידמפוטנטיות (בשונה מגנרציית tx/task).
- **useMonthlyGeneration.ts:173** — `renewal_alert_days` נשלף אך מתעלמים ממנו; חלון-החידוש קבוע-60.
- **useMonthlyGeneration.ts:141** — משימת-תיקון חוזרת מאבדת קטגוריה → follow-up ממלא 'אחר' לא 'תיקונים'.

## 🔴 ממצאי-🟢 שדילגתי עליהם (בטוחים אך דורשים בדיקה-חיה שלא יכולתי לעשות תחת מגבלת-ההוצאה)
- **FinancesV2.tsx:639** — מחיקת-תנועה נגישה-מקלדת: `.finv-tx-actions` הוא `opacity:0`-עד-hover,
  אז כפתור נגיש שם צריך גם חשיפה ב-focus-within. דורש בדיקה-חזותית.
- **Insurance.tsx:235** — קיצוץ שם-חברת-ביטוח ארוך: הפרדן ` · ` בתוך אותו span הופך את זה לעדין;
  ערך-נמוך (שמות ארוכים נדירים). דורש בדיקה-חזותית.
- **index.css (--accent-soft/--*-soft)** — הטוקנים לא-מוגדרים; כל שימוש נופל ל-rgba קשיח → dark
  mode שבור. **ה-fallbacks משתנים** (.05/.06/.09 במקומות שונים) — להגדיר אותם דורש החלטת-ערכים
  זהירה לשני ה-palettes; לא לנחש. (מתחבר ל-index.css:4891 למעלה)
- **useTransactions.ts:63** — שליפה-לא-בסדר (החלפת-חודש מהירה מציגה חודש שגוי). תיקון-מרוץ
  אמיתי אך ריפקטור-קונקרנטיות נוגע-כסף — דורש בדיקה-חיה. תיקון: פרמטר `isCurrent` שמגדר כל כתיבת-state.

## ⚠️ הסקירה לא הושלמה
14 ממצאים נשארו **לא-מאומתים** — סוכני-האימות שלהם נכשלו על מגבלת-ההוצאה (AuthContext, push.ts,
format.ts, quickParse, taskFollowup, extractFinancing, screenLabel, editContext, TasksV2,
ExpenseSheet ועוד). לא הוחלו ולא נשפטו. **שווה סבב-אימות נוסף כשהמכסה מתאפסת.**

## ענפים (🔴 — לא מוזגו)
- **`feat/extract-rate-limit`** (`26dda67`, נדחף) — הגבלת-קצב פר-בעלים לפונקציות-החילוץ: מיגרציה 037
  (`extract_usage`) + `_shared/rateLimit.ts` (20/שעה, 429 בעברית, נכשל-פתוח על תקלת-תשתית) + חיווט
  ל-4 הפונקציות. **פעולתך:** `supabase db push` (037) + `supabase functions deploy extract-*`.
- **`fix/onboarding-atomicity`** — התכנון המלא מוכן (ליבה: אל תנקה טיוטה / אל תכריז "הכול מוכן!"
  בכשל-חלקי; בטיחות-ניסיון-חוזר דרך מגני-קיום כדי לא לשכפל חלקים שכן-נשמרו). לא נבנה בריצה זו
  בשל מגבלת-ההוצאה. 🔴 [שנינו] — דורש ביקורתך ממילא.

## פעולות-בעלים ממתינות (מלא)
1. `supabase db push` — מיגרציות 033–037 (משוב + RLS 035/036 + extract_usage 037).
2. בדיקת-`relrowsecurity` שכל טבלה עם RLS מופעל.
3. `supabase functions deploy notify-feedback` + `extract-mortgage extract-loan extract-rental extract-contract`.
4. לסקור 2 הענפים (`feat/extract-rate-limit`, ולבנות/לסקור `fix/onboarding-atomicity`).
5. 12 ממצאי-הסקירה למעלה + 4 המדולגים + 14 הלא-מאומתים.
6. 26 ממצאי-הלילה הקודמים (R#/N#/W#/V#).
